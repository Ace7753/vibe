services:
  vibe:
    build: .
    container_name: vibe
    ports:
      - "8080:8080"
    volumes:
      - ./app:/app/app
      - ./downloads:/app/downloads
      - ./archives:/app/archives
    environment:
      PORT: 8080
      PYTHONUNBUFFERED: "1"
    restart: unless-stopped
